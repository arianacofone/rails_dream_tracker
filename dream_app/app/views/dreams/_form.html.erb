<%= form_for @dream, html: { class: "form" } do |f| %>
  <%= f.label "title", class: "title" %>
  <%= f.text_field :title, class: "title-field" %>

  <%= f.label "dream", class: "title" %>
  <%= f.text_area :dream, class: "dream-field" %>

  <%= f.submit %>
<% end %>
